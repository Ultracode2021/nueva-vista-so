import { createTheme } from '@mui/material/styles';
import { purple } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: light: '#757ce8',
  }
});

export defaul theme;
